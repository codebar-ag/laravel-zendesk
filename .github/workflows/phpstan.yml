name: PHPStan

on:
  push:
    paths:
      - '**/*.php'
      - 'phpstan.neon.dist'
      - 'phpstan-baseline.neon'
      - 'composer.json'
      - 'composer.lock'
  pull_request:
    paths:
      - '**/*.php'
      - 'phpstan.neon.dist'
      - 'phpstan-baseline.neon'
      - 'composer.json'
      - 'composer.lock'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  phpstan:
    name: phpstan
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6.0.2

      - name: Setup PHP
        uses: shivammathur/setup-php@2.37.0
        with:
          php-version: '8.5'
          coverage: none

      - name: Install Composer dependencies
        run: composer update --no-interaction --prefer-dist --no-ansi

      - name: Run PHPStan
        run: ./vendor/bin/phpstan --error-format=github
